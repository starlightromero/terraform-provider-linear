---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "linear_release_stage Resource - terraform-provider-linear"
subcategory: ""
description: |-
  A custom stage in a Linear release pipeline. Custom stages are inserted between the system-managed Started and Released stages. Each pipeline has four fixed lifecycle stages (Planned, Started, Released, Canceled) that cannot be created or deleted via the API.
---

# linear_release_stage (Resource)

A custom stage in a Linear release pipeline. Custom stages are inserted between the system-managed Started and Released stages. Each pipeline has four fixed lifecycle stages (Planned, Started, Released, Canceled) that cannot be created or deleted via the API.

## Example Usage

```terraform
resource "linear_release_stage" "qa" {
  name        = "QA Verified"
  color       = "#4cb782"
  position    = 1000
  pipeline_id = linear_release_pipeline.production.id
}

resource "linear_release_stage" "staging" {
  name        = "Staging"
  color       = "#3b82f6"
  position    = 2000
  pipeline_id = linear_release_pipeline.production.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `color` (String) Color of the stage as a HEX string (e.g. `#22c55e`). Each stage can have a unique color displayed in the Linear UI.
- `name` (String) Name of the release stage. Can be any descriptive name (e.g. "QA", "Staging", "Production").
- `pipeline_id` (String) Identifier of the release pipeline.
- `position` (Number) Position of the stage within the pipeline as a float value. Linear uses fractional positioning for ordering (e.g. 970.71, 2008.73). Stages are ordered by position between the system Started and Released stages.

### Optional

- `frozen` (Boolean) Whether the stage is frozen. Syncs will not automatically add issues to frozen stages.

### Read-Only

- `id` (String) Identifier of the release stage.
- `type` (String) Type of the stage. Always `started` for user-created stages. The system-managed lifecycle stages (Planned, Started, Released, Canceled) are not accessible via the API.


